# Exclude static HTML files from redirect (must come BEFORE catch-all)
/timer.html   /timer.html   200

# Only redirect non-API routes to React app
/app/*    /index.html   200

# Catch-all MUST be last
/*        /index.html   200
